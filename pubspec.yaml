name: finaltask
description: UTH SmartTasks App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  sqflite: ^2.3.0
  path: ^1.8.3

flutter:
  uses-material-design: true